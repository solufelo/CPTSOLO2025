# Frontend — copy to .env.local for local development
# Auth routes proxy to Python API via vite (port 8081)

# Set true when running: npm run dev:api (in a second terminal)
VITE_ENABLE_AUTH=false

# Leave empty when using Vite proxy (/api → localhost:8081)
VITE_API_URL=
